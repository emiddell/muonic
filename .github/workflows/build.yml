name: Build

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifact: muonic-linux
          - os: macos-latest
            artifact: muonic-macos
          - os: windows-latest
            artifact: muonic-windows

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
      - run: uv sync
      - run: uv run pyinstaller muonic.spec
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: dist/muonic/

  release:
    if: startsWith(github.ref, 'refs/tags/')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Zip dist directories
        run: |
          cd artifacts
          for dir in */; do zip -r "${dir%/}.zip" "$dir"; done

      - run: gh release create "${{ github.ref_name }}" artifacts/*.zip --generate-notes
        env:
          GH_TOKEN: ${{ github.token }}
